// can we refactor to have a generic list?

import React from "react";

const PeopleList = () => (
    <main role="main">
        <div>
            <p>Im a list</p>
        </div>
    </main>
);

export default PeopleList;
