describe('Testing CSV UI interaction', function () {
    /*
    describe('as a loggedin admin user', function () {

        beforeEach(function () {
            browser.get('/login');
            element(by.model('email')).sendKeys('admin@ush.com');
            element(by.model('password')).sendKeys('admin');
            element(by.css('button[type="submit"]')).click();


            element(by.css('.settings-nav span.settings-nav-button')).click();
            element(by.css('.main-nav a[href="/settings/data-import"]')).click();

        });

        afterEach(function () {
            // Clear localStorage to reset session
            browser.executeScript('window.sessionStorage.clear();');
            browser.executeScript('window.localStorage.clear();');
        });

        describe('When user opens page and selects a form type', function () {
            beforeEach(function () {

            });

            it('should have 2 available form types to select', function () {

            });

            it('should set the selected form type as the current selection' function () {

            });

            describe('when the user clicks on browse', function () {
                beforeEach(function () {

                });

                it('should display a file selection dialog', function () {

                });

                describe('when the user selects a file', function () {
                    beforeEach(function () {

                    });
                    it('should set No file selected to the name of the file', function () {

                    });

                    describe('when the user clicks import csv', function () {
                        beforeEach(function () {

                        });

                        it('should display a progress spinner', function () {
                            beforeEach(function () {

                            });

                            it('should show an upload confirmation', function () {

                            });

                            it('should clear the selection and browse inputs' function () {

                            });
                        });
                    });
                });
            });
        });
    });
*/
});
